// cases = int(input())
// ans= []
// def two_distinct(lst):
//     return len(set(lst))==2
// for case in range(cases):
//     num= int(input())
//     arr= list(map(int, input().split()))
//     k=1
//     for k in range(1, max(arr) + 2):
//         arr2 = [x % k for x in arr]
//         if two_distinct(arr2):
//             found_k = k
//             break
//     ans.append(found_k)
// for a in ans:
//     print(a)
#include <stdio.h>

int main(){
    int cases, num;
    scanf("%d", &cases);
    int arr[num];
    for (int i=1; i>0; i++){
        num=0;
        scanf("%d", &num);
    }
}